@model Shop.Data.Models.Car

<div class="col-lg-4">
    <img class="img-thumbnail" src="@Model.img" alt="@Model.name" />
    <h2>@Model.name</h2>
    <p>@Model.shortDesc</p>
    <p>Ціна: @Model.price.ToString("C")</p>
    <p><a class="btn btn-warning" href="#">Деталі</a></p>
</div>